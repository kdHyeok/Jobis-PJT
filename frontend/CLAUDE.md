# frontend 작업자 진입점

@AGENTS.md

## Claude Code

- 색을 새로 박지 말고 `src/styles/base.css` 의 팔레트 토큰이나 `color-mix` 파생을 쓴다.
- 움직이는 배경 위 글자는 최악 프레임 기준으로 대비를 확인한다.
- 검사를 추가할 일이 생기면 `Jenkinsfile` 이 아니라 `frontend/ci-checks` 를 고친다.
