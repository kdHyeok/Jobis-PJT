# 2026-07-31 — 멀티에이전트 백로그 M1~M8 완주 + 라우터 Opus5 + 멀티턴 하네스 (D67~D76)

## 발단 (실측 사고 2건)
1. 이력서를 붙여넣었는데 career_chat 이 즉흥 코칭·판정("서류 통과 가능성…")을 생성 —
   도구 없이 답했고, 사용자는 화면에서 출처를 구분할 수 없었다.
2. 공고 URL+이력서+요청을 한 메시지로 보내니 이력서를 인지하지 못해 적합도까지 못 갔다.

## 오늘 내린 결정 (상세는 docs/decisions.md)
- **D67** 응답 문장별 화자(replySources)를 계약→백엔드 metadata→프론트 배지로 노출
- **D68** 표지어 없는 이력서 붙여넣기 승격(detect_kind) + 합격 예측 우회 표현 금지
- **D69** 혼합 메시지(URL+이력서+요청)를 엔진이 성분별 승격 — 메시지는 플래너에 보존
- **D70** 턴 진행 궤적(progress)을 응답에 실어 채팅 UI "진행 과정" 타임라인으로
- **D71** 판정 턴에 방금 제출된 자료의 정리 단계를 결정론 삽입(병렬 실행)
- **D72** 요청 완수 보장 — blockedRequests 신고·pendingRequest 기억·자료 도착 시 재큐
- **D73** M3(request_not_fulfilled 경고)·M5(정리 도구 빈 산출 경고)·M7(요약이 원문 안 덮음)
- **D74** 라우팅 판정 티어 신설 — 플래너는 Opus 5(claude-opus-5), CLI 실호출 확인
- **D75** /v1/chat/stream(NDJSON) + 워커 stage 중계(미지원 폴백) + career_chat 1000자 격리
- **D76** consistency 하네스를 멀티턴 시나리오(S8·S9)로 확장

## 측정 (전부 Claude CLI — GMS 미사용)
| 측정 | 결과 | baseline |
|---|---|---|
| 플래너 48케이스×3 (sonnet, D72 프롬프트) | 정확 0.9653 · blocked 신고 1.0 · 회귀 없음 | planner_baseline_claude_blocked.json |
| 플래너 48케이스×3 (**Opus5 라우터**) | 정확 0.9653 · **stable-wrong 0** · 확신 0.896 | planner_baseline_claude_opus_router.json |
| **멀티턴 9시나리오×3** | **이탈 0건** — S8·S9 포함 전부 3/3, S5(KNOWN FAILURE) 안정화 관측 | consistency_baseline_multiturn.json |

상세: docs/eval-records/2026-07-31_planner-blocked-requests.md

## 남은 것 / 열어 둔 것
- planner `resume-missing` 계열 기대값(career_chat) 재검토 — Opus 는 선호 수집(능동 개입)을
  선호. 기대값이 낡았을 수 있음(OPEN, 결정 필요).
- S5 KNOWN FAILURE 주석 정리는 재관측 후.
- 회원/커리어 저장소 쪽 통합 시나리오는 백엔드 팀과 논의.

## 추가 (같은 날 오후) — D77 resume-missing 재검토
기대값 재검토 결과 **기대값 유지**(07-29 판정 유효: 자산 0의 기능 요청은 career_chat).
기록된 처방(정의 좁히기)을 실행 — career_chat/preference_intake manifest 수정.
재측정: fit-missing-both·roadmap-missing 1.00 회복, 전체 0.9792, 파손 0.
resume-missing 은 0.33 으로 OPEN 유지(오답 방향은 개선).

## 추가 2 — D78: resume-missing 완치
preference_intake 의 "이력서를 자연스럽게 요청" 훅 제거(공고 추천 전용으로 좁힘) →
resume-missing **1.00 안정·정답**(07-29 OPEN 종결). 전체 0.9861, 일관 오답 0, 파손 0.
관측: recommend-paraphrase-2 가 1/3 흔들림(첫 관측, 지속 시 대응).

## 추가 3 — D79: 조회 질문에 재분석 금지 (+ 대안 추천 경위 확인)
"공고 필수 항목 뭐였지?" 재질문에 파싱이 통째로 재실행되던 것 수정 — posting_analysis
파싱 캐시(해시 무효화) + career_chat 에 postingFacts/analysisFacts 사실 컨텍스트.
대안 공고 문구("격차를 감안한 대안")는 fit_analysis 내부 find_alternatives 노드
(중/하 등급 자동 분기, 2026-07-21 설계)이며 RAG 어댑터 훅으로 동작 중(현재
LocalPostingsRagAdapter 폴백) — 유지 결정, 실 RAG 연결은 오늘 15시 예정.

## 추가 4 — D80: 문장 속 스킴 없는 채용 사이트 주소 승격
"saramin.co.kr/… 공고분석해줘"가 감지되지 않아 career_chat 이 "URL 못 연다"로 답하던 것
수정 — 채용 사이트 도메인 한정 토큰 규칙 추가(일반 도메인 오탐 차단). 브랜치
feat/ai/agent-hardening-0731 분기.

## 추가 5 — D81·D82: 조회 선별 답변 + 발화 지속 사실 누적
- D81: 캐시된 공고의 조회 질문에 전체 재낭독 대신 물은 항목만 답함(실패 시 폴백)
- D82: 발화에서 목표·제약·상황을 추출해 세션 user_facts 누적 → 플래너 그라운딩·대화 컨텍스트

## 추가 6 — D83: 로그 라벨을 실제 행위와 일치 + 캐시 무효화 짝 맞춤
- 캐시 턴은 "분석 자료 검토", 저장 정보 참조는 "이전 대화 검토"로 진행 로그 구분
- 새 공고 제출 시 posting_summary 즉시 무효화(이전 공고 사실로 답하는 구멍 차단)

## 추가 7 — D84: 화이트보드 완성 (지식 공유 봉합 3건)
보드→그래프 시드(fit 재파싱 제거), 그래프→보드 승격(파싱·프로필), 보드→자기 루프
(user_facts 를 자소서·면접·선호가 공유). 판정 턴에서 공고 재파싱 ~50초 절감 기대.

## 추가 8 — D86: 공고 라이브러리 (두 번째 공고가 첫 공고를 지우던 문제)
posting_library(상한 5)에 정리했던 공고 전부 보관 → career_chat 이 이전 공고 질문에 답함.
활성 공고 전환(이전 공고로 판정 재개)은 OPEN.

## 추가 9 — D87: recall 경로도 공고 라이브러리를 본다
두 공고 질문에서 이전 공고 "정보 없음" 오답 — 저장은 정상, posting_recall payload 가
활성 공고만 싣던 배선 누락. 라이브러리 전체를 recall 에 공급.

## 추가 10 — D88: 여러 공고 각각 적합도 판정
targets 인자 + 라이브러리 시드 + 도구 내부 반복. analysis 자산은 활성 공고 판정만 승격.
재측정 회귀 없음(0.9861 유지). 다음: RAG(feat/ai/rag) 통합 — 계획 수립 완료.

## 추가 11 — D89: 실 RAG 연결 완료
RAG/ 사본 체크아웃(무수정) → WSL PG16 jobrag 복원(6,120공고·임베딩 100%) → 서버 기동
(모델 6.5GB·warmup 890s) → HttpRagAdapter(RAG_PROVIDER=http, 실패 시 키워드 폴백) →
E2E 통과(어댑터 3건 score 1.0 / 풀턴 추천 5건+연차 필터 고지). 루트 CLAUDE.md §2.5 추가.

## 추가 12 — D90: RAG 폴백 명시
폴백 결과에는 "실시간 검색 서버(RAG) 미연결 — 저장 공고 키워드 검색" 문구를 추천·대안
표현 모두에 명시(문구도 사실대로 — LLM 추천이 아니라 키워드 검색).

## 추가 13 — 멀티에이전트 확장 계획 실행 (D91·D92)
P3 병렬 고정+수치 로그, P1-a application_plan→job_recommend 읽기 전용 위임(실공고가
지원 경로에 실림). 안 하기로 3건(P2-b·P1-b·P2-a)은 근거와 함께 D92 로 종결.

## 추가 14 — D93: 진행 로그에 루프·위임·데이터 흐름 전부 표시
입력 자산(agent_start)·세션 넘김(agent_end)·루프 스텝별 도구 호출/관찰(agent_step)·
에이전트 간 위임과 받은 데이터(delegate/refused)를 진행 타임라인·스트리밍에 매핑.

## 추가 15 — D94: 다중 공고 URL 병렬 접수
한 발화의 URL 여러 개(상한 3)를 전부 접수 — 첫 URL 활성, 나머지 병렬 수집·파싱 후
라이브러리 승격, 답변에 각 정리 블록 표시. D88(각각 적합도)과 직결.

## 추가 16 — D95: RAG 타임아웃 오판 수정
16:38 폴백의 원인은 서버 다운이 아니라 30초 타임아웃(서버는 결국 200 완료 — 로그 대조).
타임아웃 90초 + 10초 초과 지연 WARNING 계측.

## 추가 17 — D96: RAG 캐시 선제 예열 (사용자 제안)
공고 파싱/이력서 정리 시점에 자산 기반 쿼리를 백그라운드로 1회(결과 폐기) — 실제
검색 시점의 콜드 비용을 미리 지불. 비차단·실패 무시·HTTP provider 한정.

---

## 오늘 총괄 (하루 마감 요약)

**결정 D67~D96 (30건) · 커밋 25건 push · 테스트 506→547건 · 브랜치
docs/ai/agent-guide-0730 → feat/ai/agent-hardening-0731**

축별로:
- **인테이크**: 혼합 메시지(URL+이력서+요청) 성분별 승격(D69), 표지어 없는 이력서(D68),
  문장 속 채용 사이트 주소(D80), **다중 URL 병렬 접수**(D94)
- **요청 완수**: blockedRequests 신고→pendingRequest 기억→재큐(D72), 턴 종료 검증(D73-M3),
  제출 자료 정리→판정 시퀀스(D71), **여러 공고 각각 판정**(D88)
- **화이트보드**: 파싱 캐시(D79)·공고 라이브러리(D86)·recall 배선(D87)·user_facts(D82)·
  그래프↔보드 양방향(D84)·요약 덮어쓰기 금지(M7)
- **관찰 가능성**: 화자 출처 배지(D67)·진행 타임라인(D70)·실시간 스트리밍(D75)·
  라벨 정합(D83)·루프/위임/데이터 흐름 표시(D93)·병렬 소요 계측(D91)
- **모델·평가**: Opus5 라우터(D74), 멀티턴 하네스 S8·S9(D76, 이탈 0), 플래너 0.9861
  (resume-missing 완치 D77·D78), 재측정 4회 전부 Claude CLI
- **RAG**: 실 연결(D89, 6,120공고 의미 검색)·폴백 명시(D90)·타임아웃 진단(D95)·
  선제 예열(D96). 트러블슈팅 문서에 진단 방법론 기록.
- **확장 계획**: P1-a 위임 실행(D91), 안 하기로 3건 근거 종결(D92)

열린 것: recommend-paraphrase-2 flaky 지속 여부(다음 재측정), S5 KNOWN FAILURE 주석 정리
(재관측 후), 활성 공고 전환(D86 한계), RAG 지연 계측 데이터 축적(D95).
