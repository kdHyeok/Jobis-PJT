# 2026-07-30 — 공고 URL 인입 (D62~D65)

> 브랜치: `feat/ai/posting-url-intake` (feat/ai/web-bridge 에서 분기, 사용자 지시).
> 이날 같은 브랜치에 다른 세션의 작업도 실렸다 — feat_url 수집기(D61, 925694e)와
> v2bridge(D63, d3bc10b·d3fc8ee). 여기는 URL 인입 세션의 기록만.

## 한 일 (커밋 순)

| 커밋 | 내용 |
|---|---|
| 7b5b8d6 | **URL 인입 3규약 (D62)** — 발화 URL 결정론 감지(정규식, LLM 없음) · URL 은 공고 전용(이력서 kind 로 온 URL 첨부도 공고로 고정) · 원문 승격(`ensure_posting_text`, sourceUrl 보존, 실패 시 자산 유지+경고) |
| 8cd9395 | 연차 룰: 연도 꼬리 오탐 수정 — "2025년 차세대"가 25년 차로 읽혔다(실측 Gno=49546576). 숫자 왼쪽 `(?<!\d)` + '차' 뒤 `(?![가-힣])` |
| 34f226a | 잡코리아 추천공고 티저 절단 — 광고가 연차 근거·기술스택(Java·Linux)을 오염시켰다(실측 2공고) |
| 4129e60 | 사이드바 "지원자격" 라벨이 본문 "자격 요건" 섹션을 가로채던 결함 — 우대사항 헤더 앞의 가장 가까운 매치를 본문으로. 필수요건 0→11건(Gno=49675900) |
| 2305cd0 | HTML 추출을 블록 단위 줄로 재작성 + 표 행 `셀 \| 셀` 결합 — 한글/워드 내보내기 공고의 span 파편화·표 붕괴(Gno=49638104) |
| 7a6dbb3 | 내용 없는 섹션 분리 불신 — 목차 라벨("1. 필수사항 2. …")이 "3." 두 글자를 필수 섹션으로 확정하던 것. 필수요건 0→3건 |
| ffe5c26 | **posting_fetch 도구 (D64)** — 수집/파싱 분리. `AgentSpec.internal`(플래너 manifest 제외 → 재측정 불필요), 오케스트레이터가 URL 미수집 시 큐 맨 앞 삽입, 관찰 규칙 ③(수집 실패 → 공고 소비 단계 제외), internal 도구는 병렬 그룹 제외 |
| 41a8b9a | **캡쳐 이미지 VLM (D65)** — extract 의 file 분기가 이미지를 Clova VLM 으로 텍스트화. 인라인 계약 라이브 확정: `dataUri.data` 에 `data:<mime>;base64,` **접두 포함**(문서 예시 모양은 400) |
| fcf46fe | 연차 룰: bare "무관" 오탐("학력무관"→0년) 제거 + "경력 : 1년" 라벨 표기 추가. 같은 URL 이 쿼리 파라미터에 따라 다른 템플릿(2.3k/25.7k자)을 내리는 것 실측 |

## 실측 (실공고 3건, GMS 실 모델)

| 공고 | 결과 |
|---|---|
| 위로보틱스 Gno=49546576 | 연차 25→**5년**(버그 수정), 필수 6건. 원문 11k자 |
| 에이아이스페라 Gno=49675900 | 필수 0→**11건**([팀장]/[팀원] 라벨), 광고 오염 기술 19→17개, 원문 4.2k→2.7k자 |
| 이이넥서스 Gno=49638104 (최난도: 표 공고+광고 23k자) | 필수 0→3건, 연차 무관→**1년**. **캡쳐 경로가 크롤링보다 우수**: 사용자 실제 캡쳐(1.png)로 필수 6건·우대 4건, VLM 19.3초·풀턴 37.7초 |

- 궤적: `planner 원안 [posting_analysis] → 삽입 [posting_fetch, posting_analysis] → 승격 → 파싱`, 재수집 0회
- 수집 실패 시: render "본문을 붙여넣어 주세요" + 관찰 노트 "공고 분석은 진행하지 못했어요" + followUp 카드. **캡쳐 안내는 문구에 안 넣었다** — 채팅 첨부가 아직 텍스트만 실어 날라 못 지키는 약속이 된다(D65 "남은 배선")

## 남긴 것 / 알게 된 것

- **캡쳐 업로드 배선(AI 밖)**: 프론트 업로드 → 백엔드 저장 → 첨부(sourceType=file, 경로). 이력서 업로드(`webbridge.extract_uploaded_file`)와 같은 관. **백엔드 협의 필요** — 연결되면 posting_fetch 실패 문구에 캡쳐 안내 한 줄 추가
- 캡쳐 경로의 필수/우대 **컬럼 매핑은 표 레이아웃에 좌우**된다(내용 누락은 0, 라벨이 일부 섞임)
- 워드 내보내기 표 공고는 중첩 레이아웃 표 탓에 본문이 한 줄로 뭉친다 — LLM 이 읽을 수는 있으나 세부(비자·JLPT)가 요건 카드에 빠질 수 있음(OPEN)
- 테스트 431→**493건**(+21 이 세션, +29 v2bridge 등), 전부 1.5초·LLM 0원
