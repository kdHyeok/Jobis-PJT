# 지금 상태 (자동 로드 — 짧게 유지할 것)

> `CLAUDE.md` 가 임포트한다. **여기는 "지금 어디인가" 만.** 무엇을 왜 했는지는
> `작업로그/다음작업.md`(인수인계 상세)와 `작업로그/<날짜>.md`(그날의 기록)가 출처다.
> 20줄을 넘기면 상세로 밀어낸다 — 자동 로드는 매 세션 비용이다.

- **브랜치**: `feat/ai/agent-hardening-0731`. MR·머지는 **명시 요청 시에만**.
- **스택**: 서비스 v2 풀스택 — Vue(5173) + Spring(8080) + AI v2bridge(8000) +
  **PostgreSQL(55432)** + RAG 검색(8765, 선택). **MySQL·webbridge·정적 HTML 은 폐기됐다.**
  기동 순서·명령은 리포지토리 루트 `CLAUDE.md`.
- **LLM**: `.env` `LLM_PROVIDER=claude_code`. 라우팅(플래너)은 Opus 5 티어(D74) — **nano 급으로
  내리지 말 것**(실측 붕괴 100%→48.8%). GMS 는 크레딧 절약으로 중단. 평가 하네스는 **큰 분기마다
  한 번만.**
- **테스트**: `pytest -q` — **638건 통과**. **커밋 상태**: ~**D128**(로컬, 미push) —
  프로토타입 2.0.0 이식 4건(D125 추천 지목 · D126 되묻기 · D127 trace 파일 · D128 세션 조회),
  상세 `0803.md`.
- **플래너 기준선(08-02 OPEN → 08-03 재측정)**: D125 manifest 변경 재측정에서
  **정확 1.0 · 안정 1.0 · unstable 0** (52×3, `planner_baseline_rec_targets.json`) —
  08-02 의 flaky 2건이 재현되지 않았다. D111 부모 커밋 대조는 여전히 안 했다(§2-H).
- **출시 뒤 계측(D117)**: `python scripts/harvest_sessions.py` — `eval/` 5종은 사전 정의
  케이스만 잰다. 첫 실측 275턴에서 `career_chat` 고정 안내문 **7건이 전부 정상 요청**.
  **출시 후 첫 주에 이걸 먼저 돌린다.** 다음 후보·출시 전 결정 2건도 §2-H.
- **RAG(8765) 전에 WSL 을 붙잡는다** — 유휴 60초에 내려간다. `wsl.exe -u root -e sleep 100000 &`.
- **제네릭 점수 문턱: runs=5 에서 0.0178**(D109), 기준점 **+0.0285**. 최근 상세 `0801`·`0802.md`.
- **코드 바꾸면 AI 브릿지(8000)를 반드시 재시작한다** — reload 없이 띄운다.
