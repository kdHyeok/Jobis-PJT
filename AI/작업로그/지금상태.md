# 지금 상태 (자동 로드 — 짧게 유지할 것)

> `CLAUDE.md` 가 임포트한다. **여기는 "지금 어디인가" 만.** 무엇을 왜 했는지는
> `작업로그/다음작업.md`(인수인계 상세)와 `작업로그/<날짜>.md`(그날의 기록)가 출처다.
> 20줄을 넘기면 상세로 밀어낸다 — 자동 로드는 매 세션 비용이다.

- **브랜치**: `feat/ai/roadmap-followup` (develop 기준). MR·머지는 **명시 요청 시에만**.
- **ⓐ 전환 완료(D152)**: 자산 블롭(`agent_session_state.state`)이 진실의 출처. 세션 전체가
  `collected.outputs.session_state` 로 나가고 `career.sessionState` 로 돌아온다 — 번역 없음
  (번역기 3개 삭제). 투영(지도 재료·로드맵 자동 생성)은 유지. **백엔드가 블롭을 통째로
  upsert·회신하는지 실경로 확인 필요** — 남은 일은 `0803-인수인계-에이전트스키마전환.md` §4.
- **끝까지 돈다(08-04 01:13 실측, D156)**: 이력서 적재 → 공고 → 적합도 분석 `SUCCEEDED` →
  역량 요건 34건 → **커리어지도 v2 PUBLISHED(노드 17·간선 17)**. 지도는 재료가 생기면
  자동 생성(D146). 첨부 버튼과 채팅 본문이 같게 동작.
- **막힌 것**: 없음(08-04). 인수인계 §4 의 미해결 4건을 닫았다 — 좀비 잡 회수(D157) ·
  로드맵 초안 중복(D158) · 발화 절단(D159, 결함 아님) · 실경로 지도(D156).
  **남은 것은 플래너 성능 실측 하나** — 상세는 인수인계 문서 §4-5.
- **GMS 금지**: `EMBED_PROVIDER=null` 로 띄운다(유일한 GMS 경로가 임베딩이다).
- **스택**: Vue(5173) + Spring(8080) + AI v2bridge(8000) + PostgreSQL(**도커 `jobiss-pg`,
  5436** — 옛 55432 는 폐기) + RAG(8765). 기동은 인수인계 §3·§5.
- **LLM**: `LLM_PROVIDER=claude_code`. 플래너는 Opus 5 티어(D74) — **내리지 말 것**.
- **테스트**: `pytest -q` **763건**. 백엔드 `gradlew test` 28건(도커가 있으면 RLS 통합도 돈다).
- **자산 유실 방지 장치**: `v2bridge/persistence_map.py` + `tests/test_persistence_map.py`.
- **DB 는 빈 새 볼륨**(옛 데이터 복구 불가). 검증 계정 `verify@jobiss.local` / `Verify-Password-1234!`
- **코드 바꾸면 그 서버를 반드시 재시작한다** — 전부 reload 없이 띄운다.
