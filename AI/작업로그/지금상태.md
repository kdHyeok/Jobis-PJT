# 지금 상태 (자동 로드 — 짧게 유지할 것)

> `CLAUDE.md` 가 임포트한다. **여기는 "지금 어디인가" 만.** 무엇을 왜 했는지는
> `작업로그/다음작업.md`(인수인계 상세)와 `작업로그/<날짜>.md`(그날의 기록)가 출처다.
> 20줄을 넘기면 상세로 밀어낸다 — 자동 로드는 매 세션 비용이다.

- **브랜치**: `feat/ai/posting-url-intake` (web-bridge 에서 분기 — URL 인입·캡쳐 VLM, D62~D65).
  MR 은 **명시 요청 시에만**.
- **LLM**: `.env` `LLM_PROVIDER=claude_code` (sonnet / 경량 haiku). GMS 는 크레딧 절약으로 중단.
  평가 하네스는 **큰 분기마다 한 번만** 돌린다.
- **테스트**: `uv run --extra dev --extra prototype pytest -q` — 전부 통과 (LLM 없이 1초대).
- **서버 재기동**: 백엔드는 **반드시 이 체크아웃(`S15P11C202-ai/backend`)에서** 띄운다 —
  메인 저장소와 같은 MySQL 을 쓰는데 브랜치가 다르면 스키마가 어긋나 대화 생성이 500 난다
  (루트 `CLAUDE.md` §3 경고 참고). AI 브릿지는 코드 바꾼 뒤 반드시 재시작.
- **커밋 상태**: 깨끗함 (07-30 URL 인입 세션 커밋 완료).
- **최근 마무리한 것**: 공고 URL 인입 전체 — 발화 URL 결정론 감지 → posting_fetch 도구
  (플래너 비노출, 오케스트레이터 삽입) → 원문 승격 → 파싱. 캡쳐 이미지 VLM 경로 라이브 검증.
  실공고 3건 실측으로 룰 버그 5건 수정. 상세: `작업로그/0730.md`.
- **다음 후보**: 캡쳐 업로드 프론트·백엔드 배선(협의 필요, `0730.md` 남긴 것) ·
  `다음작업.md` §2-D(열린 질문) · §2-E(로컬 LLM 성능 테스트).
- **주의**: 플래너 모델을 nano 급으로 내리지 말 것(실측 붕괴 — `다음작업.md` §1).
