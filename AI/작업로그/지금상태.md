# 지금 상태 (자동 로드 — 짧게 유지할 것)

> `CLAUDE.md` 가 임포트한다. **여기는 "지금 어디인가" 만.** 무엇을 왜 했는지는
> `작업로그/다음작업.md`(인수인계 상세)와 `작업로그/<날짜>.md`(그날의 기록)가 출처다.
> 20줄을 넘기면 상세로 밀어낸다 — 자동 로드는 매 세션 비용이다.

- **브랜치**: `feat/ai/roadmap-parser` (develop 기준). MR·머지는 **명시 요청 시에만**.
- **되는 것**: 대화에 공고(URL·원문)를 붙이면 → 공고 페이지 적재 → 분석 작업 →
  `/v1/analyses/stream` 진행 휠 → `competencyProposal` → 커리어지도 로드맵.
  상세·남은 것은 `0803-로드맵-스트리밍.md`.
- **GMS 금지**: `EMBED_PROVIDER=null` 로 띄운다(유일한 GMS 경로가 임베딩이다).
- **스택**: Vue(5173) + Spring(8080) + AI v2bridge(8000) + PostgreSQL(55432) +
  RAG(8765, 선택). **MySQL·webbridge·정적 HTML 은 폐기.** 기동은 루트 `CLAUDE.md`.
- **LLM**: `LLM_PROVIDER=claude_code`. 플래너는 Opus 5 티어(D74) — **nano 로 내리지 말 것**
  (실측 100%→48.8%). 평가 하네스는 큰 분기마다 한 번만.
- **테스트**: `pytest -q` **710건**. 백엔드 `gradlew test` 통과.
- **플래너 기준선**: 정확 1.0 · 안정 1.0 · unstable 0 (52×3,
  `planner_baseline_rec_targets.json`). D111 부모 커밋 대조는 아직(§2-H).
- **출시 뒤 계측(D117)**: `python scripts/harvest_sessions.py` — 출시 첫 주에 먼저 돌린다.
- **RAG(8765) 전에 WSL 을 붙잡는다** — 유휴 60초에 내려간다(`wsl.exe -u root -e sleep 100000 &`).
- **제네릭 점수 문턱**: runs=5 에서 0.0178(D109), 기준점 +0.0285. 상세 `0801`·`0802.md`.
- **코드 바꾸면 AI 브릿지(8000)를 반드시 재시작한다** — reload 없이 띄운다.
