
섹션별 부합도

#: 1
템플릿 기준: 개요·왜 Agent인가
부합도: 🟢 부합
근거: 단일 LLM 호출  기·검증 재시도로
요청별 경로가 갈림
────────────────────────────────────────
#: 2
템플릿 기준: 사용자 시나리오(서로 다른 경로)
부합도: 🟡 부분
근거: 파이프라인이 대체로 선형(parse→profile→gap→roadmap→verify).
분기는 있으나 제한적 가까운 편
────────────────────────────────────────
#: 3
템플릿 기준: 기능 요구사항(입출력 행동)
부합도: 🟢 부합
근거: AnalyzeRequest→AnalyzeResponse 계약으로 입출력 행동 명확
────────────────────────────────────────
#: 4
템플릿 기준: 패턴 선택·근거
부합도: 🟢 부합
근거: Multi-Agent + Reflection(verify 자기검증) + 노드 재지시.
역할분담·자기검증 근거 성립
────────────────────
#: 5
템플릿 기준: 동작 명세
부합도: 🟡 부분
근거: 입출력 스키마 ✅ / 예외 흐름 ✅(강점) / 종료조건 △(전역 max-step
미명시) / 정상흐름 T
────────────────────────────────────────
#: 6
템플릿 기준: Tool 명
부합도: 🟢 부합(철학)
근거: "예외 대신 결과객체 반환" 원칙 정확히 일치(extract/structured/RAG
 모두 warnings 반환) 드 고정
────────────────────────────────────────
#: 7
템플릿 기준: 데이터셋
부합도: 🟢 부합
근거: mock 여부 명확(키없음/NullRAG), mock 스키마=실제 스키마 유지. RAG
실데이터·평가셋은 소규모
────────────────────────────────────────
#: 8
템플릿 기준: 성공 판
부합도: 🟡 부분
근거: 경로검증·예외 성 △ (정량 평가
하네스는 있음)

---
✅ 잘 되어 있는 것

1. "예외보다 결과객   (섹션 6 핵심).extract_text·run_structured·RagResult 모두 예외를 던지지 않고 warnings를 반환 → 상위가 대처. 템플릿 71~72줄 요구사항 그대로입니다.
2. 예외 처리 체크리  충족 — 오늘 넣은 3중 방어: 호출 3회 재시도 → 실패 시 정직한 빈 결과(가짜 금지) → 오케스트레이터 노드별 재지시. "실패 시 재시도하는가?"에 Yes.
3. 자기검증(Reflection) — verify_result가 환각·금지표현·정합성 검사재시도 신호. 템플릿  충족.
4. 정량 성공 기준(섹션 8)을 실제 하네스로 — F1·근거성·정책·로드맵 준수를 숫자로 측정. 대부분의 팀이 못 하는 부분.
5. 계약·mock 관리 — Pydantic + JSON 스키마, mock/실제 스키마 동일 유지.

🔧 개선해야 할 것 (우선순위 순)

1. 종료 조건 전역 명시 (섹션 5·8-효율성) — 지금은 노드별           캡(MAX_GEN_RETRY=1,  프를 막지만, 그래프전역 recursion_limit/max-step이 명시·문서화되지 않음. LangGraph    기본값(25)에 암묵 의6 step 이내 종료"보장을 문서화.
2. 경로 다양성 관점 정리 (섹션 2) — 템플릿은 "요청마다 경로가 갈려야 Agent"라고 강조하는데, 우리는 대체로 선형입니다. 두 방향 중 택1: (a) 이 구조 태스크엔 선형이 적절하다는 근거를 설계서에 명시, 또는 (b) 동적 분기 추가(예: 기업정보가 필요할 때만 RAG 호출, 정보 충분성에 따라  단계 스킵).
3. 정상 흐름 Trajectory 문서화 (섹션 5) — toolLog에 from→to는 남지만, "모범 경로 트레이스(Thought/Action/Observation 5~10단계)"가 문서로 없음. 성공 판정 체크나리오로 작성 권장.
4. 성공 판정 5항목 ↔ 테스트 1:1 매핑 (섹션 8) — 테스트 44개는 있으나, 템플릿의 5개 체크리스트(경로/예외/결과/효율/일관)에 대응하는 항목이
명시적이지 않음. 특 은 LLM 비결정성때문에 미검증.
5. 이 템플릿 형식의 설계서가 아직 없음 — 우리 설계 문서(에이전트 설계/…plan.md)는 20장짜리 상세본이지만, 이 8섹션 템플릿에 맞춘 요약 설계서는 없음. 발표·평가용으로 이 형식 1부를 만들면 정합성 점검에 좋습니다.

---
한 줄 총평

"예외 처리·자기검증·잘 갖춰졌고(특히 오늘 작업으로 예외 흐름이 크게 강화됨), 약한 곳은 "동적 경로 다양성"과 "종료 조건·Trajectory의 명시적 문서화" 입니다.

원하시면 바로 착수할 수 있는 걸 골라주세요:
- ① 종료 조건 명시(recursion_limit + 문서) — 코드 작업, 금방 됩니다
- ② 이 템플릿 형식의준으로 8섹션 채우기)
- ③ 동적 분기 추가(조건부 RAG 호출 등)

---------------------------------------------------------------
📄 취업 컨설팅 에이전트 RAG 기반 구조 분석
1. 개요

본 시스템은 채용공고와 사용자 정보를 기반으로
맞춤형 취업 전략을 제공하는 멀티 에이전트 기반 AI 시스템이다.

전체 구조는 Graph 기반으로 설계되어 있으며,
각 노드는 독립적인 역할을 수행하고 상태(GraphState)를 통해 데이터를 전달한다.

2. 전체 파이프라인 구조
채용공고 입력
    ↓
parse_job_posting
    ↓
build_user_profile
    ↓
analyze_gap
    ↓
plan_roadmap
    ↓
(find_alternatives) [옵션]
    ↓
verify_result
3. 현재 구조 평가
✅ 설계 의도
RAG 기반 의사결정 시스템 지향
LLM은 보조 역할로 제한하려는 구조
⚠️ 실제 구현 상태
핵심 로직 대부분이 LLM에 의존
DB / RAG 활용 거의 없음
4. 노드별 LLM 의존도 분석
4.1 parse_job_posting
현재 구조
run_structured(...)
상태
❗ 완전 LLM 의존
문제점
공고 구조화 전부 LLM 수행
기술 스택 / 직무 분류도 LLM이 판단
개선 방향 (RAG)
tech stack → DB 매핑
role category → taxonomy 매핑
seniority → rule 기반 판단
4.2 normalize_job_schema
상태
✅ 비LLM (rule 기반 처리)
문제점
기술 스택 표준화 없음
synonym 처리 없음
개선 방향
ReactJS → React
Node.js → Node

→ 기술 스택 taxonomy DB 필요

4.3 build_user_profile
상태
❗ 완전 LLM 의존
문제점
사용자 스킬 추출을 LLM이 수행
경험 → skill 매핑도 LLM
개선 방향 (RAG)
skill taxonomy 기반 추출
키워드 매칭 + 룰 기반 처리
4.4 analyze_gap (핵심 문제 영역)
현재 구조
LLM이 요구사항 충족 여부 판단
문제점
판단 로직 전체를 LLM에 위임
일관성 및 신뢰도 낮음
❌ 현재 구조
LLM → 요구사항 해석 + 사용자 비교 + 판단
✅ 이상적 구조
[DB / Rule 기반]

for req in requirements:
    if req.skill in user.skills:
        fulfilled
    else:
        gap
[LLM]
→ 설명만 생성
4.5 plan_roadmap
상태
⚠️ LLM 중심
문제점
학습 경로 전부 LLM 생성
실제 데이터 기반 아님
개선 방향 (RAG)
학습 자료 DB
프로젝트 템플릿 DB
난이도 / 기간 정보 포함
4.6 find_alternatives
상태
⚠️ LLM 기반 생성
문제점
실제 취업 시장 반영 안됨
허구 직무 생성 가능
개선 방향
채용공고 DB 기반 추천
유사 직무 매칭
4.7 verify_result
상태
✅ 비교적 적절
특징
결과 검증 역할
rule + LLM 혼합 가능
5. 핵심 문제 요약
🚨 현재 구조 문제
1. Gap 분석을 LLM이 수행
가장 중요한 로직이 비결정적
2. Skill 표준화 부재
매칭 정확도 저하
3. Roadmap 데이터 기반 부족
실행 가능성 낮음
6. RAG 적용 핵심 영역
6.1 Skill / Requirement 매핑 DB
Python → programming_language
Spring → backend_framework
MySQL → database
효과
정확한 매칭
일관성 확보
6.2 Gap 분석 로직 분리
[DB 기반]
- 요구사항 충족 여부 판단

[LLM]
- 설명 생성
6.3 Roadmap 데이터베이스
"Spring Boot 부족" →

추천:
- 강의 A
- 프로젝트 B
- 실습 C
6.4 Alternative Job 추천 DB
Backend → DevOps / Data Engineer / API Engineer
7. LLM vs RAG 역할 분리
❌ 현재 구조
LLM =
- 판단
- 매칭
- 생성
✅ 목표 구조
[RAG / DB]
- 판단
- 매칭
- 계산

[LLM]
- 설명
- 요약
- 자연어 생성
8. 현재 구조의 강점
✅ Graph 기반 구조
확장성 높음
RAG 연결 용이
✅ 상태 기반 설계
데이터 흐름 명확
✅ Retry 시스템
production 대응 가능
9. 결론
현재 상태

👉 LLM 중심 취업 컨설턴트

목표 상태

👉 데이터 기반 취업 전략 엔진 + LLM은 설명만 담당

🔥 핵심 한 줄

“판단은 데이터가 하고, LLM은 말만 하게 만들어야 한다”

🧠 에이전트 6개 분석
1️⃣ parse_job_posting (채용공고 파서)
🧩 하는 일
공고 텍스트 → 구조화된 JSON
required / preferred 나누기
tech stack 추출
직무 카테고리 추론
⚙️ 사용 기술
extract_text → 텍스트 추출 (비LLM)
run_structured → LLM 기반 구조화
system prompt 강하게 있음
🤖 LLM 개입

👉 매우 높음 (핵심 로직 전부 LLM)

📉 RAG 사용 여부

❌ 없음

🔎 평가
사실상 정보 추출기 = LLM 파서
RAG로 충분히 대체 가능한 영역인데 아직 안됨
2️⃣ build_user_profile (사용자 프로필 생성)
🧩 하는 일
사용자 입력 → 정규화된 프로필
기술, 경험, 프로젝트 정리
⚙️ 사용 기술
run_structured (LLM)
일부 규칙 기반 normalization 가능성
🤖 LLM 개입

👉 높음

📉 RAG

❌ 없음

🔎 평가
이것도 거의 전부 LLM
사실:
이력서 파싱 → 룰 기반 + embedding 가능
지금은 LLM 의존 과다
3️⃣ analyze_gap (갭 분석)
🧩 하는 일
공고 vs 사용자 비교
부족한 요구사항 도출
Gap 리스트 생성
⚙️ 사용 기술
run_structured → LLM
비교 로직도 프롬프트 내부
🤖 LLM 개입

👉 매우 높음 (핵심 판단 영역)

📉 RAG

❌ 없음

🔎 평가
여기서부터 LLM “판단”이 시작됨
원래 이상적인 구조:
requirement 매칭 → embedding / rule 기반
지금은:
👉 “LLM이 다 판단”
4️⃣ plan_roadmap (로드맵 생성)
🧩 하는 일
부족한 Gap 기반
학습 계획 / 커리어 로드맵 생성
⚙️ 사용 기술
run_structured → LLM 생성
🤖 LLM 개입

👉 최고 수준 (100% 생성)

📉 RAG

❌ 거의 없음

🔎 평가
가장 LLM 의존적인 단계
원래 이상:
RAG로 학습 경로 DB에서 가져오기
지금:
👉 “LLM이 invented roadmap 생성”
5️⃣ find_alternatives (대체 경로 탐색)
🧩 하는 일
다른 직무 / 우회 커리어 제안
⚙️ 사용 기술
run_structured → LLM
일부 RAG 가능성 (adapter 있음)
from jobis_ai.rag import get_rag_adapter

👉 하지만 실제 활용 거의 없음

🤖 LLM 개입

👉 높음

📉 RAG

⚠️ 구조만 있고 실제 활용 약함

🔎 평가
여기가 RAG 써야 하는 핵심 지점인데 안 쓰고 있음
예:
유사 직무 DB
실제 커리어 패턴
현재:
👉 LLM이 “추측”으로 대체경로 생성
6️⃣ verify_result (검증기)
🧩 하는 일
결과 검증
오류 / 위반 탐지
retry trigger
⚙️ 사용 기술
LLM 기반 검증 (VerifierResult)
일부 rule (retry, status)
🤖 LLM 개입

👉 중간 ~ 높음

📉 RAG

❌ 없음

🔎 평가
중요한 역할인데
👉 “LLM이 LLM 결과를 검증하는 구조”

→ 신뢰도 문제 있음

⚠️ 핵심 문제 요약
❌ 1. RAG 거의 안 쓰고 있음
adapter는 있음
하지만 실제 retrieval 거의 없음
❌ 2. LLM이 “데이터 없이 판단”

특히:

gap 분석
대체 경로
로드맵

👉 전부 hallucination 위험 영역

❌ 3. 구조는 RAG-ready인데 실행은 LLM-first

즉:

아키텍처는 잘 짜여 있음
실제 구현은 반대로 감
📊 LLM 개입 강도 표
에이전트	LLM 의존도	RAG 사용
parse_job_posting	🔴 매우 높음	❌
build_user_profile	🔴 높음	❌
analyze_gap	🔴 매우 높음	❌
plan_roadmap	🔴 최고	❌
find_alternatives	🟠 높음	⚠️ 약함
verify_result	🟡 중간	❌
🎯 결론

👉 지금 시스템은

❌ "RAG 기반 시스템"이 아니라
✅ "LLM 파이프라인 + 약한 구조화"

🚀 개선 방향 (핵심만)
1. gap 분석 → RAG 기반으로 바꿔야 함
requirement embedding
user skill embedding
cosine match

👉 LLM 제거 가능

2. roadmap → DB 기반 추천으로 전환
스킬 → 학습 경로 매핑
LLM은 정리만
3. alternatives → 반드시 RAG 사용
직무 그래프
커리어 트랜지션 DB
4. parser만 LLM 유지

👉 이건 LLM 써도 OK