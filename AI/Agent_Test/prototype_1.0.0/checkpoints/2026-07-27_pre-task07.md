# 점검 리포트 — task 07 진입 전 (2026-07-27)

> **범위:** task 01~06 완료 시점 (툴 3개 완성).
> **방식:** AGENTS.md(계약) + 실제 실행(라이브 스모크) 교차 점검 — 단일 문서 대조가 아니라 드리프트까지 잡음.
> **결론:** 🟢 **task 07 진입 OK.**

## ① 코드 인벤토리
4개 모듈 14파일 — `schemas/`(5) · `llm/`(3) · `preprocess/`(2) · `tools/`(4). 크기 적정(최대 `analyze_gap.py` 145줄).

## ② AGENTS 계약 대조 — 🟢 GREEN
- 모델문자열·`init_chat_model`·GMS 연결 → **`llm/`에만** (§6 격리 준수)
- LLM 창구 사용 → `preprocess`·`tools/analyze_gap`만 (`load_posting`·`search_postings`는 결정적 = LLM 안 씀, 맞음)
- `tools/`에 명시적 `raise` 없음 (실패 = `ToolError` 반환)
- `.env` 미추적 (GMS_KEY 안전, `.env.example`만 커밋)

## ③ 라이브 회귀 스모크 — 🟢 PASS (합성 성공)
end-to-end 체인 물림:
```
이력서 → profile(28스킬) → search_postings(추천 3건) → URL 재조회(search→load 라운드트립)
       → analyze_gap(level=하) → 관찰 분기 → 대체직군 검색(3건)
```
**드리프트/회귀 없음.** 5부품이 실제 데이터로 다 맞물려 돌아감.

### ⚠️ 발견 1 — mock 검색 ↔ 갭분석 불일치 (예상됨, 실패 아님)
추천 top(SI 공고)이 검색 score 1위였으나 갭분석은 **하(0.0)**. 원인: mock 검색은 스킬 키워드가 본문에 **있기만** 하면 매칭(얕음), 갭분석은 **실제 요건 커버리지**를 봄. → mock의 한계로, **실제 RAG가 개선**. 프로토타입 스코프상 정상.

### ⚠️ 발견 2 — uncertain 과다 시 level 취약
위 케이스: decided 1건(missing) + **uncertain 4건** → score가 소수 항목에 좌우돼 하. 판정불가가 많으면 level 근거가 얇아짐. **품질 범위 밖**(정직한 판정불가 처리 결과)이나 **task 08 성공판정 시 인지**할 것.

## ④ 정본 vs 현실 — 🟢
툴 3개·이력서 파이프라인·**관찰 분기 재료**(level·missing_required) 모두 실현. 남은 것 = **분기 실행(agent 루프)** 하나.

## ⑤ task 07 준비도 — 🟢 맞물림
- 툴 시그니처 준비: `load_posting(url)→Posting|ToolError` · `search_postings(q)→SearchResult` · `analyze_gap(p,post)→GapResult|ToolError`
- `RunAgentResult{reply, meta{scenario, tools_called, level}}` 스키마 존재
- **task 07 할 일:** LangGraph 조립 + 라우팅(D9 동적) + 관찰분기 + reply·meta 생성
- **남은 게이트(D9):** 동적 라우팅 ↔ 결정적 level 분기 **결합 방식**
- **통합 포인트:** 툴이 Pydantic 객체 반환 → LangGraph tool 노드 태우는 방식(직렬화) 결정 필요

## 종합
🟢 **부품·계약·합성 다 건강, task 07 진입 OK.** 발견 2개는 mock/품질 스코프 내 예상 항목(실패 아님).
