# 점검 리포트 — 프로토타입 1.0.0 개발·검증 완결 (2026-07-27)

> **결론:** 🎉 커리어 코치 Agent **프로토타입 1.0.0의 개발·검증 완결.** FastAPI(09)는 고도화로 연기(D13).
> 이 문서 = 1.0.0 마무리 스냅샷 / 고도화·팀 핸드오프 기준점.

## 완료 태스크 (01~08·10)
| # | 산출 | 상태 |
|---|---|---|
| 01 스키마 | `schemas/` (profile·공고·툴 I/O·run_agent 반환) | ✅ |
| 02 llm 창구 | `llm/` (GMS OpenAI-compat, 티어 매핑) | ✅ |
| 03 이력서 파서 | `preprocess/` (docx→profile) | ✅ |
| 04 load_posting | `tools/load_posting.py` (URL→공고, NOT_FOUND) | ✅ |
| 05 search_postings | `tools/search_postings.py` (RAG **Mock**) | ✅ |
| 06 analyze_gap | `tools/analyze_gap.py` (갭분석 상/중/하) | ✅ |
| 07 Agent 루프 | `agent/` (LangGraph B: 동적 라우팅 + 결정적 분기) | ✅ |
| 08 테스트 셋 | `tests/checks.py` | ✅ 19/19 |
| 10 Agent Eval | `tests/eval.py` | ✅ 8/8 3/3 |
| 09 FastAPI | — | ⏸ 고도화 연기(D13) |

## 검증 요약
- **자동 검사**: `python -m tests.checks [--live]` → **19/19 PASS**
- **Agent Eval**: `python -m tests.eval --runs 3` → 판정 8케이스 **3/3 일관, 이탈 0**
- **수동 대화**(chat.py): S1~S4 정상 + **S3 상 → 성공기준 3 런타임 검증**
- → **성공기준 5개 전부 런타임 시연 완료** (AGENTS §8)

## 실행 방법
```
python run.py                 # 시나리오3 스모크
python chat.py                # 대화형 (직접 질의)
python -m tests.checks        # 자동 검사 (크레딧 0)
python -m tests.checks --live # + 라이브
python -m tests.eval --runs 3 # Agent Eval
```

## 아키텍처 한눈에
```
자연어 쿼리 + (사전파싱)profile
 → agent(LLM bind_tools, 동적 라우팅)
   ⇄ tools(load_posting / search_postings[mock] / analyze_gap)
   ─[조건엣지] analyze_gap & level 중/하 → auto_search(대체직군)
 → RunAgentResult{reply, meta{tools_called, level, run_id}}
```
- 툴 3개 고정 · 싱글턴 · GMS(OpenAI) · run_id 로깅

## 고도화 로드맵 (→ 상세 decisions **D14**)
- **1.0.1** (구조 유지): 다중 입력 강건화(여러 **텍스트** 이력서·다양 케이스 라우팅) · `analyze_gap` 정교화(동의어·연차) · **mock 검색 품질 개선** (텍스트 이력서 가정 유지)
- **1.1.0** (구조 진화): 멀티턴 대화 상태 · Agent Eval 심화(LLM-judge) · `analyze_gap` 정교화 **지속**
- **후순위(far)**: FastAPI(Java 연결)
- **유보(계획 아님)**: 표 이력서 파싱(텍스트 가정 벗어날 때만)
- **우리 스코프 밖**: 실제 RAG 구현(RAG팀) · OCR 이미지공고(데이터팀/크롤러) — ※ mock 검색 개선은 1.0.1(우리)

## 잔여 약점 (알려진·스코프 내)
- S1 추천 정밀도 = mock 검색 얕음(키워드). analyze_gap 분석은 정확. → 실제 RAG가 해결.

## 문서·거버넌스
- `AGENTS.md`(코딩 헌법, CLAUDE.md 자동로드) · `decisions.md`(D1~D13) · `tasks/`(01~10) · `checkpoints/`(3) · `test_logs/`(3)
