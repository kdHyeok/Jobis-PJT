# 수집 결과물은 용량 문제로 git에 올리지 않는다 (전달 방식은 팀 협의)
data/
exports/

# 자동 실행(run_daily_update.py) 로그와 잠금 파일 — 로컬 실행 기록이라 공유 대상이 아니다
logs/

# 로컬 가상환경
.venv/

# Python bytecode
__pycache__/
*.py[cod]

# 자동 실행(run_daily_update.py) 로그와 잠금 파일 — 로컬 실행 기록이라 공유 대상이 아니다
logs/
