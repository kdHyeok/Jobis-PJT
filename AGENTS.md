# JOBIS 개발 작업 지침

이 파일은 `C:\JOBIS` 아래에서 작업하는 모든 사람과 AI 에이전트가
작업을 시작하기 전에 따라야 하는 저장소 규칙이다.

## 통합 작업공간 원칙

- 이 폴더는 `C:\jobiss-service-v3-integration-lab`을 복사해 만든 새 통합 작업공간이다.
- 최신 팀 에이전트의 비교 기준은 `C:\S15P11C202`의 `develop` 브랜치이고, 기존 v3 기능의
  비교 기준은 `C:\jobiss-service-v3-integration-lab`이다. 두 원본은 이 폴더에서 수정하지 않는다.
- 최종 목표는 최신 팀 에이전트를 기반으로 v3의 프로젝트 설계·원자 역량·Capability Graph·
  로드맵 기능을 내부 모듈로 통합한 **단일 JOBIS AI 서버**다.
- `AI-v3`는 통합이 끝날 때까지 기능 비교와 회귀 확인을 위한 원본으로 보존한다. 별도 서버 두 개를
  최종 구조로 고정하지 않는다.
- 현재 격리 자원은 PostgreSQL `58432/jobiss_v3_integration_lab`, 단일 JOBIS AI `8400`,
  Capability Graph `8600`, 백엔드 `8380`, 프론트엔드 `5473`이다. 과거 AI v3의 8500 실행 경로는
  D047 구현에서 제거했으며 다시 추가하지 않는다.
- 실제 AI에 없는 값은 어댑터에서 지어내지 않는다. 기능 부재나 계약 불일치는 명시적인
  오류 또는 `null`로 노출한다.
- AI의 결과는 제안으로 취급하고, 사용자의 확인 없이 커리어 저장소·공고·로드맵을 변경하지 않는다.
- 에이전트 기능을 늘리더라도 기존 채팅·공고 분석·로드맵 계약의 호환성을 유지한다.

## 작업 원칙

- 기존 작업 트리의 변경은 사용자와 다른 팀원의 작업일 수 있으므로 보존한다.
- 사용자가 명시적으로 요청하지 않으면 Git 커밋, Push, 브랜치 변경을 하지 않는다.
- 원본 작업공간을 이 통합판에서 수정하거나 실행 데이터·포트를 공유하지 않는다.
- 수정 전에 관련 프론트엔드, 백엔드, AI 서버와 DB 계약을 함께 확인한다.
- 오류 수정은 사용자에게 보이는 메시지만 바꾸지 말고 실제 예외와 실패 지점을 확인한다.

## CI/CD 소유권

CI는 **뼈대와 검사 내용을 나눠서** 관리한다. 기능을 만들 때 고칠 파일이 어디인지가
이 구분으로 정해진다.

| 계층 | 파일 | 소유 | 기능 개발자가 고치나 |
|---|---|---|---|
| 뼈대 | `Jenkinsfile` | Infra | ❌ (MR + Infra 리뷰) |
| 검사 내용 | `<서비스>/ci/test.sh` | 해당 서비스 담당 | ✅ |
| 테스트·마이그레이션 | 서비스 소스 | 코드를 바꾼 사람 | ✅ |
| 배포 | `ops/`, `infra/`, `compose.yaml` | Infra | ❌ (MR + Infra 리뷰) |

- **`Jenkinsfile`이 정하는 것**: 스테이지 구성과 순서, 브랜치 게이트(`when`), 실행 컨테이너
  이미지, 서비스 컨테이너(PostgreSQL 등), 자격증명, 이미지 승격, 프로덕션 배포.
- **`<서비스>/ci/test.sh`가 정하는 것**: 그 서비스에서 무엇을 검사하는가.
  `backend/ci/test.sh`, `AI/ci/test.sh`, `frontend/ci/test.sh`, `RAG/ci/test.sh`.

### 기본 규칙

- 기능을 추가·수정할 때 **기본 동작은 CI를 고치는 것이 아니라 CI에 걸리도록 테스트를 쓰는 것**이다.
  기존 파이프라인이 새 테스트를 자동으로 주워 담는다.
- 자기 기능을 통과시키려고 게이트를 느슨하게 만들지 않는다. `--frozen`을 떼거나, 실패하는
  테스트를 `@Disabled`로 막거나, 스테이지의 `when` 조건을 바꿔 자기 브랜치를 건너뛰게
  하는 변경은 리뷰에서 되돌린다.
- **CI 실패는 원인을 만든 사람이 고친다.** 실패를 남긴 채 다른 MR을 쌓지 않는다.
- 새 인프라 의존성(Redis, 메시지 브로커 등)이 통합 테스트에 필요하면 `Jenkinsfile`에
  서비스 컨테이너를 추가하는 MR을 올리고 Infra 리뷰를 받는다. 서비스 스크립트 안에서
  `docker run`으로 몰래 띄우지 않는다.
- 리뷰 책임자는 `.gitlab/CODEOWNERS`에 있다. 그 파일의 핸들은 자리표시자이므로 실제
  GitLab 사용자명으로 바꾼 뒤 protected branch에서 code owner 승인을 켠다.

### 게이트별 목적

- **기능 브랜치 / MR**: 이 코드가 공유 브랜치에 들어가도 남을 깨뜨리지 않는가.
  컴파일, 단위 테스트, DB 마이그레이션, 핵심 통합.
- **develop**: 합쳐진 시스템이 도는가. 여기서 여섯 이미지를 불변 SHA 태그로 빌드·푸시한다.
- **master**: 이 아티팩트를 배포해도 되는가. `Master release: verify`가 master 트리와
  검증된 develop 부모 트리가 **동일한지** 강제한다.
- master에서는 **다시 빌드하지 않는다.** develop에서 검증한 이미지를 그대로 승격한다
  (Build Once, Deploy Many). 이 원칙을 깨는 변경은 리뷰에서 되돌린다.

## 작업 상태와 압축 복구

- Codex 작업은 가능하면 작업공간과 현재 디렉터리를 `C:\JOBIS`로 열어 시작한다. 그래야 이 파일과
  프로젝트 로컬 `.codex/config.toml`이 적용된다.
- `AGENTS.md`에는 장기간 유지할 규칙만 기록하고, 현재 작업의 진행 상황은 루트 `TASK.md`에 기록한다.
- 작업을 시작하거나 대화 압축 후 재개할 때는 `TASK.md`와 그 문서가 가리키는 결정 문서를 먼저 읽는다.
- 대화 요약과 `TASK.md`가 충돌하면 실제 파일, 테스트 결과, Git 상태 순으로 사실을 다시 확인한다.
  확인된 저장소 상태가 우선이며 차이를 사용자에게 알린다.
- 중요한 결정, 단계 완료, 새 차단 요인, 작업 종료 또는 수동 `/compact` 직전에 `TASK.md`를 갱신한다.
  사소한 명령 기록과 긴 로그를 계속 누적하지 않는다.
- `TASK.md`에는 비밀키, 토큰, 전체 환경 변수나 개인정보를 기록하지 않는다.
- 검증하지 않은 작업을 완료로 표시하지 않는다. 실행한 검증 명령과 결과를 `TASK.md`의
  `Verification`에 짧게 남긴다.
- 현재 작업이 끝나면 `TASK.md`를 다음 작업의 정확한 목표와 재개 지점으로 정리한다. 영구 결정은
  `docs/07-decision-register.md`로 옮기고 `TASK.md`에서는 해당 결정 번호만 참조한다.

## Spring JDBC와 PostgreSQL SQL 규칙

- `JdbcClient`와 `NamedParameterJdbcTemplate`에서 이름 있는 파라미터를 사용하는
  SQL에는 전통적인 `?` placeholder를 혼용하지 않는다.
- PostgreSQL JSONB 존재 연산자 `?`, `?|`, `?&`는 Spring이 placeholder로
  해석할 수 있으므로 Java SQL에서 사용하지 않는다.
- JSONB 키 존재 검사는 다음 함수로 작성한다.
  - `jsonb_exists(json_value, key)`
  - `jsonb_exists_any(json_value, keys)`
  - `jsonb_exists_all(json_value, keys)`
- null이 될 수 있는 이름 있는 파라미터를 `:value is null` 또는
  `:value is not null`로 판별하지 않는다. SQL을 분기하거나 null이 아닌 boolean
  파라미터를 별도로 전달하고 값 파라미터에는 명확한 타입의 값을 전달한다.
- 같은 파라미터를 여러 문맥에서 사용할 때 PostgreSQL이 타입을 추론할 것이라고
  가정하지 않는다. 필요하면 SQL 분기 또는 명시적 캐스팅을 사용한다.

## SQL 변경 검증

- SQL을 추가하거나 수정하면 성공 입력뿐 아니라 null 입력, 중복 입력, 캐시 적중,
  캐시 미적중, 동시 요청 경로를 검토한다.
- `SpringJdbcSqlGuardrailTest`를 통과해야 한다.
- DB 동작 변경은 실제 PostgreSQL 통합 테스트로 검증한다. CI에서 Docker가 없어
  Testcontainers 테스트가 건너뛰어졌다면 이를 성공한 통합 검증으로 표현하지 않는다.
- 마이그레이션은 가능하면 실제 PostgreSQL에서 트랜잭션으로 실행 후 롤백하여
  문법과 데이터 변환 결과를 확인한다.

## API 500 진단 순서

1. 브라우저의 일반 오류 문구가 아니라 API 응답 코드와 요청 ID를 확인한다.
2. IntelliJ 또는 서버 로그에서 최초 애플리케이션 예외를 확인한다.
3. PostgreSQL 로그에서 같은 시각의 SQL 오류를 확인한다.
4. 대상 레코드가 생성되지 않았는지, 생성 후 실패했는지 DB 상태를 확인한다.
5. 원인을 재현하는 테스트를 추가한 뒤 수정하고 전체 관련 테스트를 실행한다.
