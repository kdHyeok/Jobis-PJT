.env
.idea/
.vscode/
.DS_Store

frontend/node_modules/
frontend/dist/
frontend/.vite/

backend/.gradle/
backend/build/
backend/out/
# CI(backend/ci/test.sh)가 배포용으로 남기는 실행 JAR. Jenkins가 stash로 넘긴다.
backend/backend.jar
# 'CI plan' 이 서비스별 마지막 통과 트리 해시를 남기는 곳. 워크스페이스 로컬이며
# 없으면 전부 실행하므로(fail-open) 커밋하지 않는다.
.ci-cache/

ai-server/.venv/
ai-server/.pytest_cache/
ai-server/.ruff_cache/
ai-server/__pycache__/
ai-server/**/*.pyc
ai-server/*.egg-info/
AI/.venv/
AI-v3/.venv/
AI-v3/.pytest_cache/
AI-v3/**/*.pyc

infra/data/
.local/
.local-copied-do-not-use/
*.log

# 에디터 로컬 설정
.obsidian/

# 세션·실행 산출물 — 커밋 대상이 아니다
.claude/worktrees/
.tmp/
.ruff_cache/
artifacts/
frontend/test-results/

# 크롤링 수집물 — 제3자 채용공고 원문은 저장소에 커밋하지 않는다(약관·저작권). 저장소 외부에서 보관.
RAG/Jobis_통합본_*/
all_job_postings.*
infra/airflow/imports/

# DB 덤프 — 실사용 스냅샷은 개인정보·제3자 공고 원문을 담으므로 커밋하지 않는다
exec/*.sql
exec/*.dump
